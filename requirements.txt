# Query-conditioned guidelines: data prep, training (verl), MATH-500 eval
# Python >= 3.10 required. For CUDA: pip install -r requirements-cuda.txt after.
# vLLM is separate: pip install -r requirements-vllm.txt (see below).

# Core
torch
transformers
accelerate
datasets
pandas
pyarrow>=6.0.0
numpy<2.0.0

# Training (verl PPO)
hydra-core
ray[default]
tensordict>=0.8.0,<=0.10.0,!=0.9.0
peft
codetiming
dill
packaging>=20.0
tensorboard
wandb
uvicorn
fastapi

# Inference / reward: install from requirements-vllm.txt (needed for MATH-500 eval).
# On ARM or when no prebuilt wheel exists, vLLM builds from source and can fail
# (e.g. compiler -march flag). Install vLLM on an x86_64 GPU node or use a
# conda/env that provides it.

# Math grading (MATH-500 eval + guidelines reward)
math-verify
mathruler

# Optional / dev
# pre-commit
# pylatexenc  # if needed for LaTeX in data
