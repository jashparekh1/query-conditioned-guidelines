# cmd to run baseline files:

## python scripts/baseline_eval/evaluate_gsm8k_baseline.py --model_path models/frozen/Qwen2.5-3B-Instruct --dataset_path data/gsm8k/test.parquet --output_path results/baseline/qwen_gsm8k_baseline_test.json --max_samples 10

rm --max_samples arg to run full dataset

# something wrong with math dataset first 10 samples -- need to look.